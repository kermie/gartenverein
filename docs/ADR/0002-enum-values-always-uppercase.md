# Enum values: always uppercase

**The bug that kept recurring:** several enums (`ParzelleStatus`,
`EinsatzTyp`, `TeilnahmeStatus`, `BefreiungsGrund`) had lowercase values in
Python (`"aktiv"`), but the very first migration (`0001_initial`) had
created the PostgreSQL enum types in **uppercase** (`AKTIV`). SQLAlchemy
sends the *value* (not the name) of a `str` enum as the parameter --
lowercase in Python plus uppercase in the DB led to
`invalid input value for enum`.

**Convention ever since:** all enum values are defined in uppercase,
identical to the enum name:

```python
class ParzelleStatus(str, enum.Enum):
    AKTIV = "AKTIV"
    GEKUENDIGT = "GEKUENDIGT"
    GELOESCHT = "GELOESCHT"
```

When creating a new migration with `sa.Enum(...)`, always explicitly
specify the desired (uppercase) string values, **never** rely on the
values auto-generated by `alembic revision --autogenerate` -- those
sometimes pick up the Python enum *names*, which happens to match, but
isn't guaranteed.

**Display still stays lowercase:** so the UI doesn't show
`AKTIV`/`GEKUENDIGT`, templates use the Jinja filter `|lower`:
`{{ p.status.value|lower }}`.

